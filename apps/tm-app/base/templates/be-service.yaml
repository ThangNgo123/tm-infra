---
apiVersion: v1
kind: Service
metadata:
  name: tm-be-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: tm-be
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
