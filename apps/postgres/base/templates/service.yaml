apiVersion: v1
kind: Service
metadata:
  name: tm-postgres-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ .Values.name }}
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
